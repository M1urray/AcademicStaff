@model IEnumerable<Latest_Staff_Portal.ViewModel.LecturerAssignedUnits>
<table id="datatable-buttons" class="table table-bordered dt-responsive nowrap" style="border-collapse: collapse; border-spacing: 0; width: 100%;">
    <thead>
        <tr>
            <th>Action</th>
            <th>Programme</th>
            <th>Semester</th>
            <th>Unit</th>
            <th>Unit Name</th>
        </tr>
    </thead>
    <tbody>
        @{
            if (Model != null)
            {
                {
                    if (Model.Count() > 0)
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td><a href="#" class="btn btn-danger" onclick="SelectedRow('@item.Code', '@item.Semester', '@item.Unit', '@item.LnNo');">Delete</a></td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Code)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Semester)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Unit)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Unit_Name)
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="6" style="color:red">
                                No lecturer Records Found
                            </td>
                        </tr>
                    }
                }
            }
        }
    </tbody>
</table>
