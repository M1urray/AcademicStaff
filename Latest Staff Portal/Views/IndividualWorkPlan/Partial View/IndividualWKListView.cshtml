@model IEnumerable<Latest_Staff_Portal.ViewModel.IndividualWorkPlan>
<div class="pre-scrollable">
    <table id="dtIndTaget" class="table table-bordered dt-responsive nowrap" style="border-collapse: collapse; border-spacing: 0; width: 100%;">
        <thead style="background-color:black;color:white">
            <tr>
                <th>Action</th>
                <th>No</th>
                <th>Appraisal Period</th>
                <th>Directorate</th>
                <th>Department</th>
                <th>Open To</th>
            </tr>
        </thead>
        <tbody>
            @{
                if (Model != null && Model.Count() > 0)
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                <input type="button" onclick="SelectedRow('@item.DocNo','@item.Status');" class="btn btn-sm btn-danger" value="Select" />
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.DocNo)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ApprisalPeriod)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Dim1)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Dim2)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Status)
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="5" style="color:red">
                            No Records Found
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>



